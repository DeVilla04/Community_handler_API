import express from "express";

const server = async (): Promise<express.Application> => {
  const app = express();

  //Loaders

  //Middlewares

  //Routes

  return app;
};

export default server;
